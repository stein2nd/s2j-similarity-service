<!--
目的：「実装状況サマリー、Backlog、品質レポート、まとめ」の明文化
-->

## S2J Similarity Service - 実装状況

本ページは、現状の実装状況を「機能単位」で一覧化し、**実装を100%にするために仕様書で何を明確化すべきか** を示します。

### 仕様書 (参照元)

* `README.md` (ユーザー向けの利用仕様)
* `docs/core/similarity_spec.md` (類似度算出の仕様)
* `docs/interfaces/usage_spec.md` (利用手順・例コードの仕様)
* `docs/interfaces/rest_api_spec.md` (REST API 仕様)
* `docs/interfaces/sdk_spec.md` (型安全 SDK 設計；見出し「**TypeScript SDK の品質保証の方針**」で TS のテスト範囲・非対象を規定)
* `docs/contracts/codegen_spec.md` (OpenAPI codegen ・生成物の扱い)
* `docs/engineering/ci.md` (見出し「**CI 品質ゲート方針**」「**OpenAPI レスポンス契約検証 (JSON Schema Validation)**」「**ドキュメント品質ゲート**」ほか「**CI 品質ゲートの段階的な拡張方針**」で HTTP 実レスポンスと OpenAPI の整合、Markdown lint、将来フェーズを規定；ジョブ別コマンドは「CI マトリックス」)
* `docs/engineering/codegen_pipeline.md` (見出し「**生成 PHP DTO の品質・整形ポリシー**」で生成 DTO の lint/整形と CI との関係を規定；現行リポジトリでは**未実装**)
* `docs/engineering/build_and_release.md` (見出し「**TypeScript SDK Runtime Validation 配布ポリシー**」で runtime 検証の optional 配布を規定；現行リポジトリでは**未実装**)
* `schema/openapi.yaml` (OpenAPI: API 契約の起点)

※ `docs_old/` 配下は「仕様執筆の参考資料」であり、**実装状況の評価・完了条件の根拠には使用しません。**

### 機能一覧 (実装状況サマリー)

| 機能名 | 実装済み/未実装 | 実装％ | 完了条件 (実装％を100にするために仕様書で明確化すべき点) |
|---|---:|---:|---|
| 類似度算出 (2文)`SimilarityService::similarity()` | 実装済み | 100 | - **docs/core/similarity_spec.md**: cosine + 0..1正規化 + clamp の整合。追加の明確化なし |
| 類似度算出 (1対多)`SimilarityService::similarityOneToMany()` | 実装済み | 100 | - **docs/core/similarity_spec.md**: 出力の順序保証 (入力順維持) の整合。追加の明確化なし |
| 類似度算出 (N 対 N 行列)`SimilarityService::similarityMatrix()` | 実装済み | 100 | - **docs/core/similarity_spec.md**: 行列定義 (対称/対角) の整合。追加の明確化なし |
| Embedding 生成 (単発)`EmbeddingService::embed()` | 実装済み | 100 | - **コード**: `EmbeddingService::embed()` および依存 Strategy は実装済み (`tests/EmbeddingServiceTest.php` 等)。<br>- **ドキュメント**: **docs/interfaces/usage_spec.md** の PHP 例は `S2J\Similarity\...` に統一済み。追加の明確化なし |
| Embedding 生成 (バッチ)`EmbeddingService::embedBatch()` | 実装済み | 100 | - **docs/interfaces/sdk_spec.md**:「出力は入力順を維持」の整合。追加の明確化なし |
| OpenAI Embeddings 連携 `OpenAIEmbeddingStrategy` | 実装済み | 100 | - **コード**: コンストラクタは `apiKey` / `defaultModel` / `endpoint` / `timeoutSeconds` (`src/Infrastructure/Embedding/OpenAIEmbeddingStrategy.php`)。バッチ・エラー分岐は実装済み。<br>- **ドキュメント**: **docs/interfaces/usage_spec.md** § 設定・コード例は実装と一致。**README.md** の Usage に見出し「OpenAIEmbeddingStrategy オプション」があり、名前付き引数・コード例・オプション一覧がコンストラクタと一致 (HTTP クライアント差し替えは未実装のため記載なし)。追加の明確化なし |
| ベクトル正規化 (L2正規化) | 実装済み | 100 | - **docs/core/similarity_spec.md**:「入力は正規化済み」前提を、Strategy 側で担保する方針として明文化 (現実装と一致)。追加の明確化なし |
| キャッシュ (Embedding Decorator)`CachedEmbeddingStrategy` + `InMemoryCache` | 実装済み | 100 | - **docs/interfaces/sdk_spec.md**: キャッシュキーは正規化テキスト (trim + lower) + model (未指定は `__default__`) + provider + normalized=true を JSON 化し sha256。TTL デフォルト値は24h。追加の明確化なし |
| エラー型 (DomainError 系) | 実装済み | 100 | - **docs/interfaces/rest_api_spec.md** の命名規則どおり、`error.type` / OpenAPI enum / PHP `DomainError::$type` は snake_case に統一済み。追加の明確化なし |
| 旧 API (`S2J\\SimilarityService\\...`) の削除 | 実装済み | 100 | - **docs/interfaces/usage_spec.md** の方針どおり、旧 API の namespace と旧メソッドを削除済み。追加の明確化なし |
| テスト基盤 (PHPUnit + WorDBless) | 実装済み | 100 | - **ランタイム**: `phpunit/phpunit` **^13.1**、`phpunit.xml` は schema URL が PHPUnit **12.4** 系。<br>- **`composer.lock` をリポジトリ管理** し、`composer install` で WordPress 配置・WorDBless ドロップインまで再現可能。<br>- **`composer.json` の `autoload-dev`**: `S2J\Similarity\Tests\Support\` → `tests/Support/` (契約検証ヘルパー用)。<br>- **`tests/bootstrap.php`** で WorDBless により WordPress 実ランタイムを読み込み。<br>- **テストスイート**: `unit` は `./tests` から `./tests/Integration` のみ除外 (`tests/Support` にテストクラスは置かず、統合テストから参照)。`wordpress` は `./tests/Integration` のみ (現行 **`WordPressRestAdapterIntegrationTest` に14ケース**)。`composer test` は両スイート、`composer run test:unit` / `composer run test:wordpress` で個別実行。<br>- GitHub Actions 上のジョブ対応は下表「CI・品質ゲート」。追加の明確化なし |
| OpenAPI レスポンス契約検証 (JSON Schema・実 HTTP レスポンス) | 実装済み | 100 | - **規範・スコープ**: **docs/engineering/ci.md** § **OpenAPI レスポンス契約検証 (JSON Schema Validation)**。初期スコープは **POST `/v1/similarity` 相当**と **POST `/v1/embedding` 相当**の **200** (`SimilarityResponse` / `EmbeddingResponse`) および **`ErrorResponse`** (ドメイン由来・入力検証 **400** を含む。`WP_Error` のみの応答は OpenAPI `ErrorResponse` と別形のため対象外)。**ヘッダー・オプション項目の網羅・列挙の全組み合わせ**等は同ファイルの段階的導入でバックログ化。<br>- **実装**: `tests/Support/OpenApiResponseContractValidator.php` が `schema/openapi.yaml` の `#/components/schemas/*` を解決し `justinrainbow/json-schema` で検証 (`nullable` は検証器向けに正規化)。公開静的メソッド: `assertSimilaritySuccessBody` / `assertEmbeddingSuccessBody` / `assertErrorResponseBody`。YAML 読込は `symfony/yaml` **^6.4**、`justinrainbow/json-schema` **^6** (いずれも `require-dev`、PHP **>=8.0** と整合)。`tests/Integration/WordPressRestAdapterIntegrationTest.php` が `WP_REST_Server` 経由の実データに対してアサート (similarity: 成功・検証 **400**・ドメイン **403** / **429** (`Retry-After` 付き含む) / **408** / **503**；embedding: 成功・検証 **400**)。<br>- **CI**: **ジョブ2 (WordPress 統合)** の `composer run test:wordpress` に内包 (単独ジョブなし)。<br>- **完了条件 (スコープ内)**: 上記ファイル・テスト・依存が揃い CI で実行されること。スコープ外の拡張は **ci.md** に従い実装・仕様を追補すればよい。 |
| CI・品質ゲート (engineering / GitHub Actions) | 実装済み | 100 | - **規範**: **docs/engineering/ci.md** の見出し「**CI 品質ゲート方針**」(ゴールは契約整合・回帰検知・仕様乖離防止；品質ゲートは CI で自動実行し失敗時は merge 不可；ジョブは責務単位で分離；外部プロバイダ依存テストは CI に含めない)。責務・非責務・非対象 (プレビュー deployment・ブラウザマトリックス・負荷テスト等) は同セクション。<br>- **ワークフロー** `.github/workflows/ci.yml` が同ファイル「CI マトリックス」と整合。**php-quality**: `actions/checkout` → `setup-php` **8.4** → `composer install` → `test:unit` → `phpstan` → `phpcs`。**wordpress-integration**: checkout → `setup-php` **8.4** → `composer install` → `test:wordpress` (**OpenAPI レスポンス契約検証**を含む。詳細は上表の行)。**openapi-codegen**: checkout → `setup-node` **22** → **`Install zsh`** (`./scripts/generate/all.zsh` が zsh 前提のため) → `npm ci` → `./scripts/generate/all.zsh` → `git diff --exit-code`。**typescript-sdk**: checkout → `setup-node` **22** → `npm ci` → `npm test` / `build` / `typecheck` (いずれも `-w @s2j/similarity-client`)。**docs-lint**: checkout → `dorny/paths-filter@v3` で **`README.md` / `docs/**` / linter 設定・lock 変更時のみ** `setup-node` **22** → `npm ci` → `npm run lint:docs` (変更なしの PR ではスキップステップで成功終了。詳細は下表「**ドキュメント Lint**」)。<br>- **branch protection** (`main`): GitHub 上で **php-quality** / **wordpress-integration** / **openapi-codegen** / **typescript-sdk** / **docs-lint** を **required status checks** として設定済み (`strict: false`；PR review 必須はオフ)。<br>- **静的解析**: `phpstan.neon` (level **3**、`src` / `adapters`)。<br>- **スタイル**: `phpcs.xml.dist` (**PSR-12**、`src` / `adapters` / `tests`；OpenAPI 生成 DTO `src/Contracts/DTO/Generated/` は対象外、tests は WorDBless 都合で PSR-1 の一部を緩和)。`composer run phpcbf` / `composer run lint:php`。<br>- **ライブラリの PHP 要件**: `composer.json` の `php` は **>=8.0**；**CI の実行バージョンは 8.4**。<br>- **仕様化済み・未実装** (上記マトリックス外の拡張): **docs/engineering/ci.md** の見出し「**CI 品質ゲートの段階的な拡張方針**」および **docs/engineering/codegen_pipeline.md** の見出し「**生成 PHP DTO の品質・整形ポリシー**」。現行 `.github/workflows/ci.yml`・`phpcs.xml.dist`・生成 DTO の扱いには**未反映**。 |
| HTTP サーバー実装 (WordPress REST API Adapter) | 実装済み | 100 | - **実装済**: `adapters/http/wordpress/` にて `Routes::register` → `register_rest_route` (唯一の routing mechanism)、`SimilarityController` / `EmbeddingController`、`RequestValidator`、`ErrorMapper`、`ResponseFactory`、`BearerTokenAuth` (第2引数・`permissionDenied` による **403**)、`rate_limit` 時の **`Retry-After`** (`DomainError::$details['retry_after']` が非負整数のとき)。デフォルト namespace は `Routes::DEFAULT_NAMESPACE` (`s2j/v1`)。`Routes` クラスは **docs/interfaces/rest_api_spec.md** § REST API (HTTP Runtime / WordPress REST Adapter) を参照。<br>- **仕様反映済み**: **rest_api_spec.md** に「WordPress REST API Adapter の完成条件」および「REST API (HTTP Runtime / WordPress REST Adapter)」(OpenAPI 論理パスと WordPress runtime の関係・責務境界) が明文化されている。<br>- **(任意・ドキュメント)** rest_api_spec 内にコード現状と齟齬する「未実装」列挙が残っていれば、仕様側を現状に合わせて整理する。 |
| REST API (`POST /v1/similarity`, `POST /v1/embedding` の契約に対応する HTTP 面) | 実装済み | 100 | - **実装済**: **docs/interfaces/rest_api_spec.md** の「REST API の成立条件」および § REST API (HTTP Runtime / WordPress REST Adapter) に沿い、`schema/openapi.yaml`、`Routes` + `register_rest_route`、Controller 層、リクエスト検証、`ErrorMapper`、`tests/Integration/WordPressRestAdapterIntegrationTest.php` (WorDBless + `WP_REST_Server`、**現行 14 テストメソッド**)。多数の HTTP ステータスを統合テストで検証 (429には `Retry-After` を含む)。OpenAPI 論理パス ↔ `/wp-json/s2j/v1/...` または `?rest_route=/s2j/v1/...` は仕様・テスト (`rest_url()`) で整合。<br>- **ユーザー導線 (必須) 達成**: **README.md** に OpenAPI 論理パス、WordPress runtime URL、`rest_api_init` の登録例、Bearer 運用、curl 例を記載済み (rest_api_spec の「100% 完了条件」必須項目)。<br>- **レスポンス JSON と OpenAPI の機械照合**: 上表「**OpenAPI レスポンス契約検証**」の行 (**similarity** と **embedding** の両ルートが初期スコープ)。 |
| OpenAPI を Single Source にした codegen (TS types/Zod/PHP DTO) | 実装済み | 100 | - **実行**: `scripts/generate/all.zsh` を唯一の入口とし、`schema/openapi.yaml` から生成 (開発 / CI / release のみ)。再現性は `openapitools.json` + `package-lock.json`。<br>- **出力**: PHP DTO は `src/Contracts/DTO/Generated/` (Composer 配布)。TS types ・ Zod ・ raw client は `tools/generated/ts/` (リポジトリ追跡、**docs/contracts/codegen_spec.md** の公開境界)。Composer の Git アーカイブから TS は `.gitattributes` の `export-ignore` で除外。<br>- **CI**: **openapi-codegen** ジョブで生成差分ゼロを検証 (Runner に **zsh** を入れてから `./scripts/generate/all.zsh` を実行。詳細は上表「CI・品質ゲート」)。ローカルは `npm run verify:codegen` / `npm run build:ts-client`。<br>- **ランタイム HTTP と契約の照合**: codegen ジョブとは別。上表「**OpenAPI レスポンス契約検証**」および **WordPress 統合**ジョブ参照。 |
| TypeScript SDK (`@s2j/similarity-client`: ApiClient / HttpClient / Timeout / Retry / エラー union) | 実装済み | 100 | - **規範**: **docs/interfaces/sdk_spec.md** の見出し「**TypeScript SDK の品質保証の方針**」— 軽量ユニットテストを標準とし、ブラウザ E2E・WordPress 統合ライブ・HTTP ライブ統合は要求しない；`HttpClient` をモックして検証；タイムアウト / リトライ / エラー標準化は SDK 責務として検証；OpenAPI 契約整合はコード生成の再現性で担保；成功レスポンスの runtime 検証 (Zod 等) は**標準採用しない**；generated raw は公開 API ではなく内部実装として扱う。<br>- **実装**: 上記に整合し `packages/ts-client` に `createApiClient` / `ApiClient.similarity`・`embedding`、`HttpClient`、`withTimeout`、`withRetry` (通信失敗・タイムアウト・HTTP **429** / **503**；**http.test** で **400**・**401** の非再試行を検証。403 は実装上も再試行対象外)、`SDKError` union と `isSDKError`。生成物は `tools/generated/ts` に限定し公開面は本パッケージのみ。<br>- **テスト**: **Vitest** + モック `HttpClient` を `packages/ts-client/test/` (`client.test.ts` / `http.test.ts` / `errors.test.ts`) に配置し、リクエスト構築・タイムアウト・リトライ・エラー正規化・型ガードをカバー。**CI**: **docs/engineering/ci.md** のジョブ4どおり **typescript-sdk** で `npm test` / `build` / `typecheck` (`-w @s2j/similarity-client`)。ローカルは `npm run test:ts-client` 等。<br>- **ユーザー導線**: **README.md**・**usage_spec.md** にインストール・ビルド・`verify:codegen` を記載済み。<br>- **仕様化済み・未実装** (sdk_spec の「標準外」に対応する配布・導入の詳細): **docs/engineering/build_and_release.md** の見出し「**TypeScript SDK Runtime Validation 配布ポリシー**」(optional の runtime 検証・依存の扱い・リリース方針)。**sdk_spec** の runtime 検証方針との関係は同ファイル末尾を参照。 |
| README / 使用方法ドキュメントの整合 (命名・例コード) | 実装済み | 100 | - **README.md**: PHP の `SimilarityService` / `EmbeddingService` / キャッシュは現行 `S2J\Similarity\...` で整合。**WordPress REST アダプタ**は論理パス・`/wp-json/s2j/v1/...`、`Routes::register`、Bearer、curl まで記載済み。**TypeScript SDK** は利用・ビルド・`verify:codegen` を記載済み。Usage の「OpenAIEmbeddingStrategy オプション」により、`OpenAIEmbeddingStrategy` の公開コンストラクタが **README・usage_spec・実装の三者で一致**。<br>- **usage_spec.md**: 名前空間・コンストラクタ・例外・TS SDK 区分はコードベースと整合 (長文の細部は運用で継続レビュー)。追加の明確化なし |
| ドキュメント Lint (Markdown・textlint / `@s2j/docs-linter` npm パッケージ) | 実装済み (README 手順残) | 95 | - **規範**: **docs/engineering/ci.md** の見出し「**ドキュメント品質ゲート**」。`README.md` と **docs/** を CI 品質対象とし、ルールは **`@s2j/docs-linter`** を Single Source of Truth とする。ローカルと CI で同一コマンド (`npm run lint:docs`) を適用する。<br>- **ローカル**: **S2J Docs Linter** を npm パッケージ **`@s2j/docs-linter`** (`devDependencies`) として導入。**package.json** の `lint:docs` (`s2j-docs-linter ./README.md ./docs/**/*.md`)。ルート **`.textlintrc.json`** が `node_modules/@s2j/docs-linter/presets/base/.textlintrc.base.json` を extends し、プロジェクト固有 allowlist を上書き。**`.vscode/settings.json`** が `./.textlintrc.json` を参照 (エディター統合)。**`npm ci` / `npm install`** で依存が揃う。<br>- **CI**: `.github/workflows/ci.yml` の **docs-lint** ジョブ。`dorny/paths-filter@v3` により **`README.md` / `docs/**` / `package.json` / `package-lock.json` / `pnpm-lock.yaml` / `.textlintrc.json` / `.vscode/settings.json`** 変更時のみ `setup-node` **22** → `npm ci` → **`npm run lint:docs`**。対象外変更のみの PR ではスキップステップで成功終了。**`main` 上で CI 実行済み** (check 名 **`docs-lint`**)。<br>- **branch protection**: **`main`** の required status checks に **`docs-lint`** を含む全 CI ジョブ (**php-quality** / **wordpress-integration** / **openapi-codegen** / **typescript-sdk** / **docs-lint**) を設定済み。<br>- **完了条件 (100%)** (**ci.md** 同見出し「完了条件 (100%)」): (1) ~~品質ゲート方針の明文化~~ **済**。(2) ~~`.github/workflows/ci.yml` に docs lint job~~ **済** (`docs-lint`)。(3) ~~対象変更時のみ実行~~ **済** (paths-filter)。(4) ~~checkout / Node / dependency install / `lint:docs`~~ **済**。(5) ~~GitHub branch protection で **docs-lint** を required check に追加~~ **済**。(6) **README.md** にローカル実行手順 (`npm run lint:docs`) を明記 (**未記載**)。 |

### 補足 (現行コードから見える前提)

* 本リポジトリは **「純粋な PHP ライブラリ」** としては主要機能が実装済み (類似度計算、Embedding、バッチ、キャッシュ、基本エラー)。
* 公開 API は `S2J\Similarity\...` に統一され、`S2J\SimilarityService\...` (旧 API) は削除済みです (Composer の autoload に過去互換の接頭辞が残っていても、旧クラスは提供しません)。
* `error.type` は `schema/openapi.yaml` と `DomainError::$type` とで snake_case に統一済みです。
* HTTP runtime は WordPress REST API のみ (`register_rest_route`) とし、`adapters/http/wordpress/` に routing、Controller、認証・エラーマッピング・レスポンス整形を実装済みです。**HTTP 統合テスト**は WorDBless 上の `WP_REST_Server` / `rest_do_request()` で実行します (`WordPressRestAdapterIntegrationTest` **14 ケース**)。**README.md** には OpenAPI 論理パスと WordPress 実エンドポイント、プラグインへの組込み、コマンドラインによる REST 呼び出し例、TypeScript SDK の利用手順、および Usage の「OpenAIEmbeddingStrategy オプション」(コンストラクタの公式な簡易リファレンス) が記載されています。
* **`composer.lock`** をバージョン固定のソースとして追跡しており、clone 後は `composer install` でテスト用 WordPress 配置と WorDBless のドロップインまで再現できます。**開発専用**の Composer オートロード (`autoload-dev`) により `tests/Support/` の契約検証クラスが読み込まれます。
* **CI とエンジニアリング品質**: **docs/engineering/ci.md** の「**CI 品質ゲート方針**」が merge 前に満たすべきゲートの規範であり、**.github/workflows/ci.yml** がそのジョブ実装です。**`main`** には branch protection があり、**php-quality** / **wordpress-integration** / **openapi-codegen** / **typescript-sdk** / **docs-lint** の5ジョブが required status checks です。PHP 側は **`composer run lint:php`** (PHPStan + PHPCS) と `test:unit` / `test:wordpress` / `phpcbf` が利用可能です。**OpenAPI レスポンス契約検証**は上表の専用行のとおり `test:wordpress` に含まれ、機能一覧では **実装済み / 100%** (**ci.md** で定めた初期スコープ内)。**CI 上の PHP は 8.4** (ワークフローの `setup-php`)。
* **OpenAPI codegen**: `docs/contracts/codegen_spec.md` の Governance に従い、`npm run verify:codegen` と CI の **openapi-codegen** ジョブで生成差分ゼロを検証します。**openapi-codegen** ジョブは **zsh を apt でインストール**してから `npm ci` と `./scripts/generate/all.zsh` を実行します。**TypeScript SDK** のビルド・テスト・型チェックは **typescript-sdk** ジョブで実行します (Node **v22.x**)。**ドキュメント Lint** は **docs-lint** ジョブで **`README.md` / `docs/**` 等の変更時のみ** `npm run lint:docs` を実行します (ローカルも同一コマンド。**branch protection** 済み。**README.md** への手順明記のみ上表「**ドキュメント Lint**」の残タスク)。
* **TypeScript SDK** は npm ワークスペース `packages/ts-client` (`@s2j/similarity-client`) として実装済みです。**docs/interfaces/sdk_spec.md** の「**TypeScript SDK の品質保証の方針**」どおり、Vitest とモック HTTP で公開 API を守り、E2E や runtime スキーマ検証を標準要件とはしません。`tools/generated/ts` は内部実装とし、外部からはラッパーのみを公開面とします。**optional の runtime 検証をどう配布するか**は **docs/engineering/build_and_release.md** の「**TypeScript SDK Runtime Validation 配布ポリシー**」で仕様化済みですが、現行パッケージ・CI には**未実装**です。
* **CI の将来フェーズ**および**生成 PHP DTO の品質・整形**は、それぞれ **docs/engineering/ci.md** の「**CI 品質ゲートの段階的な拡張方針**」と **docs/engineering/codegen_pipeline.md** の「**生成 PHP DTO の品質・整形ポリシー**」で仕様化済みですが、現行ワークフロー・lint 設定には**未実装**です。
* OpenAPI には `ErrorResponse` まで定義済みです。**HTTP レスポンスの JSON Schema 機械検証**は **WordPress 統合** で実施済み (詳細は上表「**OpenAPI レスポンス契約検証**」)。**`/v1/similarity` と `/v1/embedding` の成功・代表的な `ErrorResponse` (embedding の検証 **400** を含む)** は初期スコープに含みます。**レスポンスヘッダー** や **スキーマの完全カバー** は **docs/engineering/ci.md** の段階的導入に委ねます。
